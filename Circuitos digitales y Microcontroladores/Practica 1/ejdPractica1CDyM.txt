#include <stdio.h>
#include <stdint.h>
void sleep();
void bits(unsigned char);
void bits(unsigned char num){
    unsigned char x;
    for (x=0;x<8;x++){
        printf("%d \n",num&0x01);
        num=num>>1;
        sleep(1);
        
    }
    
};
int main()
{
    
    unsigned char num=20;
    bits(num);

    return 0;
    
    
}