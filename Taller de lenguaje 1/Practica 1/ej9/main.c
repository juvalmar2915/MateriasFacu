#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;
    /*for (i = 0; i <= 11; i++)
        printf("%d\n",i);  // se imprimen todos los numeros del 0 al 11*/
    /*for (i = 0; i <= 11; ++i)
        printf("%d\n",i); // se imprimen todos los numeros del 0 al 11 ya que ++i es pre condicional pero se realiza al final el incremento*/
    /*for (i = 2; i <= 11; i+=2)
        printf("%d\n",i); // se imprimen los numeros pares a partir del 2 */
    /*for (i = 1; i <= 11; i*=2)
        printf("%d\n",i);//i se va multiplicando 2 veces y se va mostrando en pantalla hasta llegar al 11*/
    /*for (i = 11; i >= 1; i-=2)
        printf("%d\n",i);//se imprimen los numeros impares pero del mas grande al mas chico */
    /*for (i = 11; i > 1; i=+1)
        printf("%d\n",i);//imprime solo el 11 ya que entra al for pero despues i es igualado a 1 por lo que por la condicion no se cumple y sale*/
    return 0;
}
